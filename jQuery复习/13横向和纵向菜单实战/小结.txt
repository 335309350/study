padding:0;margin:0;清除缩进值
ul,li表示菜单时候，要清除缩进值
li-style：none;清除圆点
使用background-image来指定一个元素的背景图，如果背景图片比元素的实际大小要小，那么背景图就会自动在横向和纵向上重复填充。
background-repeat控制背景图的填充方式

background-position:x,y;来控制背景图片显示的位置，第一个横向，第二个是纵向。


IE6上要设置display:inline-block;表示在一行，设置宽度让a填充li



.li-main a表示.li-main元素的内部所有的a节点，.li-main>a只选择.li-main的子节点中的a节点


show(),hide()动画效果显示和隐藏，改变了高度和宽度

toggle()判断元素的显示状态，显示就是隐藏，隐藏就显示。

slideUp(),slideDown()向上隐藏，向下显示

<!doctype html>声明要写

菜单从纵向变成横向，只要浮动就可以。


多个选择器分割，只要用逗号隔开。



















